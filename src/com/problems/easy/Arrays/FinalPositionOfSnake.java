package com.problems.easy.Arrays;

import java.util.List;

/*
 * Here the problems is solve to
 * there is n x n array like snake 
 * game you give the n and array arr 
 * refers the position moving and you 
 * have return the final position of the 
 * snake 
 * 
 * Input: n = 2, commands = ["RIGHT","DOWN"]
    Output: 3
 */
public class FinalPositionOfSnake {

    // Create a static method
    private int finalPositionOfSnake(int n, List<String> commands) {
        if (n <= 0 || commands.isEmpty()) {
            return 0;
        }

        // Create a 2D snake arr
        int[][] snake = new int[n][n];
        return 0;

        // now add the row and colum

    }

    public static void main(String[] args) {

    }

}
